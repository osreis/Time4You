<% if params[:brand_id] %>
	<ul class="breadcrumb">
	  <li><a href="/brands">Marcas</a> <span class="divider">/</span></li>
	  <li class="active"><%= "Produtos da #{Brand.find(params[:brand_id]).name}" %></li>
	</ul>
<% end %>
<%= index_header %>
<%= form_tag("/products", :method => "get") do %>
  <%= text_field_tag :query, nil, :style => 'height:auto', :class => 'search-query', :placeholder => "Busca..." %>
  <button type="submit" class="btn">
  <i class="icon-search"></i> Buscar Produtos </button>
	Por:
	<%= select_tag :query_option, "<option>Código de barras</option> <option>Nome</option>".html_safe, :style => 'position:relative; top:5px' %>
<% end %>
<div style="float:right; margin-top:-8%">
	<%= link_to raw('<i class="icon-plus icon-white"></i>' + ' ' + 'Adicionar produto'), new_product_path, :class => "btn btn-primary" %>
</div>
<%= render "table" %>